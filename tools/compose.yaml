# Tools Stack - Stirling PDF, Vikunja, Actual, ntfy, Uptime Kuma, SnapOtter
# Usage: ./nova.sh up tools   (or: docker compose -f tools/compose.yaml up -d)
# Requires .env file for: NOVA_DOMAIN, TZ

name: tools

networks:
  traefik_default:
    external: true
    name: "traefik_default"

services:

  stirling-pdf:
    container_name: "stirling-pdf"
    environment:
      - "TZ=${TZ}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sS http://localhost:8080/api/v1/info >/dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      # JVM cold start under Sablier wake can push past 30s. Give the healthcheck
      # room so a fresh spin-up doesn't briefly surface as unhealthy.
      start_period: 60s
    image: "stirlingtools/stirling-pdf:latest"
    labels:
      traefik.enable: "true"
      traefik.docker.allownonrunning: "true"
      traefik.http.routers.stirling-pdf.rule: "Host(`stirling-pdf.${NOVA_DOMAIN}`)"
      traefik.http.routers.stirling-pdf.entrypoints: "websecure"
      traefik.http.routers.stirling-pdf.tls: "true"
      traefik.http.routers.stirling-pdf.middlewares: "authelia@file,sablier-stirling-pdf@file"
      traefik.http.services.stirling-pdf.loadbalancer.server.port: "8080"
      sablier.enable: "true"
      sablier.group: "stirling-pdf"
      homepage.group: "Tools"
      homepage.name: "Stirling PDF"
      homepage.icon: "stirling-pdf.svg"
      homepage.href: "http://stirling-pdf.${NOVA_DOMAIN}"
      homepage.description: "PDF Manipulation — on-demand · click to wake"
      homepage.statusStyle: "dot"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    ports:
      - "8080:8080/tcp"
    restart: "unless-stopped"
    volumes:
      - "stirling_config:/configs"

  vikunja:
    healthcheck:
      disable: true  # distroless image has no shell, wget, or curl; app health confirmed via logs
    image: vikunja/vikunja:latest
    container_name: vikunja
    environment:
      VIKUNJA_SERVICE_PUBLICURL: "https://vikunja.${NOVA_DOMAIN}"
      VIKUNJA_SERVICE_JWTSECRET: ${VIKUNJA_TOKEN}
      VIKUNJA_DATABASE_PATH: /db/vikunja.db
    networks:
      - "traefik_default"
    volumes:
      - vikunja_files:/app/vikunja/files
      - vikunja_db:/db
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.docker.allownonrunning: "true"
      traefik.http.routers.vikunja.rule: "Host(`vikunja.${NOVA_DOMAIN}`)"
      traefik.http.routers.vikunja.entrypoints: "websecure"
      traefik.http.routers.vikunja.tls: "true"
      traefik.http.routers.vikunja.middlewares: "authelia@file,sablier-vikunja@file"
      traefik.http.services.vikunja.loadbalancer.server.port: "3456"
      sablier.enable: "true"
      sablier.group: "vikunja"
      homepage.group: "Tools"
      homepage.name: "Vikunja"
      homepage.icon: "vikunja.svg"
      homepage.href: "http://vikunja.${NOVA_DOMAIN}"
      homepage.description: "Task Management — on-demand · click to wake"
      homepage.statusStyle: "dot"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"

  # Kept ALWAYS-ON (no Sablier). The actual-mcp connector below reaches this
  # server directly over traefik_default at http://actual:5006, which bypasses
  # Traefik/Sablier — so an on-demand `actual` would never be woken for an MCP
  # call. Claude reaches the connector from Anthropic's cloud at any time, so the
  # budget server has to be up whenever the MCP syncs. See context/actual-mcp.md.
  actual:
    container_name: "actual"
    healthcheck:
      test: ["CMD-SHELL", "node src/scripts/health-check.js"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 20s
    image: "actualbudget/actual-server:latest"
    labels:
      traefik.enable: "true"
      traefik.http.routers.actual.rule: "Host(`actual.${NOVA_DOMAIN}`)"
      traefik.http.routers.actual.entrypoints: "websecure"
      traefik.http.routers.actual.tls: "true"
      traefik.http.routers.actual.middlewares: "authelia@file"
      traefik.http.services.actual.loadbalancer.server.port: "5006"
      homepage.group: "Tools"
      homepage.name: "Actual Budget"
      homepage.icon: "actual-budget.svg"
      homepage.href: "https://actual.${NOVA_DOMAIN}"
      homepage.description: "Budgeting"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    restart: "unless-stopped"
    volumes:
      - "actual_data:/data"

  # Actual Budget MCP (SSE) — exposes the Actual instance to Claude as a remote
  # MCP connector, reachable from any device signed into the account.
  #
  # PUBLIC + NO Authelia on purpose: Claude reaches custom connectors from
  # Anthropic's cloud, not from the LAN, so a browser SSO (Authelia) can't sit in
  # front of this machine-to-machine API. Auth is the server's own bearer token
  # (--enable-bearer + BEARER_TOKEN); Traefik terminates TLS at the edge and the
  # actual-mcp-sse@file middleware rate-limits the route as defence-in-depth.
  # Remote tier is meant to be read-mostly: block mutating tools client-side in
  # Claude and keep backups current (context/actual-mcp.md).
  #
  # The @actual-app/api this wraps downloads a LOCAL copy of the budget into /data
  # and syncs back, so:
  #   - the actual_mcp_data volume is MANDATORY (the container won't start without
  #     a writable data dir);
  #   - the first response after a restart is slow (full budget download) — hence
  #     the generous healthcheck start_period.
  actual-mcp:
    container_name: "actual-mcp"
    depends_on:
      - actual
    image: "sstefanov/actual-mcp:1.11.3"   # pin an exact version; never :latest for financial data
    command: ["--sse", "--enable-write", "--enable-bearer"]
    environment:
      - "ACTUAL_SERVER_URL=http://actual:5006"
      - "ACTUAL_PASSWORD=${ACTUAL_PASSWORD:?set ACTUAL_PASSWORD in .env}"
      - "ACTUAL_BUDGET_SYNC_ID=${ACTUAL_SYNC_ID:?set ACTUAL_SYNC_ID in .env}"
      - "ACTUAL_DATA_DIR=/data"
      - "BEARER_TOKEN=${MCP_BEARER_TOKEN:?set MCP_BEARER_TOKEN in .env}"
      # Only needed if server auth and budget end-to-end encryption use DIFFERENT
      # passwords; harmless when unset. Leave blank in .env to disable.
      - "ACTUAL_BUDGET_ENCRYPTION_PASSWORD=${ACTUAL_BUDGET_ENCRYPTION_PASSWORD:-}"
      - "TZ=${TZ}"
    healthcheck:
      # Node is guaranteed present in the image; curl/wget are not. A bare TCP
      # connect to the SSE port proves the process is up and listening without
      # needing a bearer token. start_period covers the cold-start budget download.
      test: ["CMD-SHELL", "node -e 'require(\"net\").connect(3000,\"127.0.0.1\").on(\"connect\",()=>process.exit(0)).on(\"error\",()=>process.exit(1))'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    labels:
      traefik.enable: "true"
      traefik.http.routers.actual-mcp.rule: "Host(`actual-mcp.${NOVA_DOMAIN}`)"
      traefik.http.routers.actual-mcp.entrypoints: "websecure"
      traefik.http.routers.actual-mcp.tls: "true"
      # NO authelia@file — see comment above. The bearer token is the auth layer.
      # actual-mcp-sse@file rate-limits the public route (Traefik does not buffer
      # responses by default, so the SSE stream passes through intact).
      traefik.http.routers.actual-mcp.middlewares: "actual-mcp-sse@file"
      traefik.http.services.actual-mcp.loadbalancer.server.port: "3000"
      homepage.group: "Tools"
      homepage.name: "Actual Budget MCP"
      homepage.icon: "actual-budget.svg"
      homepage.href: "https://actual-mcp.${NOVA_DOMAIN}"
      homepage.description: "Actual Budget MCP connector (SSE)"
      # Pinned version tag → let WUD notify on a new upstream release (semver tag
      # watch). No digest watch: we deliberately track versioned tags for
      # financial data, and deploy new versions by hand after review.
      wud.watch: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    restart: "unless-stopped"
    volumes:
      - "actual_mcp_data:/data"       # REQUIRED — local budget copy; container fails without it
    # deliberately NO `ports:` — reachable only via Traefik

  ntfy:
    container_name: "ntfy"
    command: serve
    environment:
      - "TZ=${TZ}"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --tries=1 http://localhost:80/v1/health -O /dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    image: "binwiederhier/ntfy:latest"
    labels:
      traefik.enable: "true"
      traefik.http.routers.ntfy.rule: "Host(`ntfy.${NOVA_DOMAIN}`)"
      traefik.http.routers.ntfy.entrypoints: "websecure"
      traefik.http.routers.ntfy.tls: "true"
      traefik.http.services.ntfy.loadbalancer.server.port: "80"
      # No Authelia middleware — ntfy must be reachable by webhooks from Uptime-Kuma, Backrest, etc.
      homepage.group: "Infrastructure"
      homepage.name: "ntfy"
      homepage.icon: "ntfy.svg"
      homepage.href: "https://ntfy.${NOVA_DOMAIN}"
      homepage.description: "Push Notifications"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    restart: "unless-stopped"
    volumes:
      - "ntfy_data:/var/cache/ntfy"

  snapotter:
    container_name: "snapotter"
    environment:
      - "TZ=${TZ}"
      - "AUTH_ENABLED=true"
      - "DEFAULT_USERNAME=admin"
      - "DEFAULT_PASSWORD=admin"
      - "TRUST_PROXY=true"
      - "EXTERNAL_URL=https://snapotter.${NOVA_DOMAIN}"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:1349/api/v1/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    image: "ghcr.io/snapotter-hq/snapotter:latest"
    labels:
      traefik.enable: "true"
      # On-demand: sablier@file middleware wakes this container when hit and stops
      # it after 60m idle. `allownonrunning` tells Traefik to keep the router
      # registered while the container is stopped so the wake path works.
      # Middleware order matters: Authelia must run BEFORE Sablier so unauth'd
      # probes don't wake the container.
      traefik.docker.allownonrunning: "true"
      traefik.http.routers.snapotter.rule: "Host(`snapotter.${NOVA_DOMAIN}`)"
      traefik.http.routers.snapotter.entrypoints: "websecure"
      traefik.http.routers.snapotter.tls: "true"
      traefik.http.routers.snapotter.middlewares: "authelia@file,sablier-snapotter@file"
      traefik.http.services.snapotter.loadbalancer.server.port: "1349"
      sablier.enable: "true"
      sablier.group: "snapotter"
      homepage.group: "Tools"
      homepage.name: "SnapOtter"
      homepage.icon: "mdi-image-edit"
      homepage.href: "https://snapotter.${NOVA_DOMAIN}"
      homepage.description: "Image manipulation — on-demand · click to wake"
      # Green dot = running, red dot = sleeping (Sablier stopped it). See
      # patterns.md "On-Demand Container Spin-Up (Sablier)".
      homepage.statusStyle: "dot"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    restart: "unless-stopped"
    shm_size: "2gb"
    volumes:
      - "snapotter_data:/data"
      - "snapotter_workspace:/tmp/workspace"

  # Browser terminal -> host sshd. Auth layers:
  #   1. Traefik TLS at the edge
  #   2. Authelia 2FA (default policy is two_factor; inherited by `shell.` subdomain)
  #   3. host sshd (password / key / PAM-TOTP — whatever sshd is configured for)
  # No SSH credentials are stored in the image: ttyd spawns `ssh` and the user
  # types their host secret in the browser. A ttyd CVE alone yields no host access.
  # Built locally (./shell/Dockerfile) because we want a fresh ttyd base — the
  # upstream wettyoss/wetty image hasn't been rebuilt since 2022.
  shell:
    container_name: "shell"
    build:
      context: "../shell"
    command:
      - "ttyd"
      - "--writable"
      - "--port=7681"
      - "--max-clients=2"
      - "--check-origin"
      - "ssh"
      - "-o"
      - "StrictHostKeyChecking=accept-new"
      - "-o"
      - "UserKnownHostsFile=/tmp/known_hosts"
      - "-t"
      - "${SHELL_SSH_USER:?set SHELL_SSH_USER in .env to your host username}@host.docker.internal"
    environment:
      - "HOME=/tmp"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      # Use 127.0.0.1 (not localhost): ttyd/libwebsockets is built with IPv6 off
      # and binds v4 only, but busybox wget resolves localhost to ::1 first and
      # gets ECONNREFUSED without falling back to v4.
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:7681/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    labels:
      traefik.enable: "true"
      traefik.http.routers.shell.rule: "Host(`shell.${NOVA_DOMAIN}`)"
      traefik.http.routers.shell.entrypoints: "websecure"
      traefik.http.routers.shell.tls: "true"
      traefik.http.routers.shell.middlewares: "authelia@file"
      traefik.http.services.shell.loadbalancer.server.port: "7681"
      homepage.group: "Infrastructure"
      homepage.name: "Shell"
      homepage.icon: "mdi-console-line"
      homepage.href: "https://shell.${NOVA_DOMAIN}"
      homepage.description: "Browser terminal (Authelia + sshd)"
      wud.watch: "false"  # locally-built image; rebuild via `./nova.sh recreate tools shell`
    mem_limit: "128m"
    networks:
      - "traefik_default"
    pids_limit: 200
    security_opt:
      - "no-new-privileges:true"
    cap_drop:
      - "ALL"
    read_only: true
    tmpfs:
      - "/tmp:size=16m,mode=1777"
    user: "nobody"
    restart: "unless-stopped"

  uptime-kuma:
    container_name: "uptime-kuma"
    environment:
      - "TZ=${TZ}"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:3001/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    image: "louislam/uptime-kuma:latest"
    labels:
      traefik.enable: "true"
      traefik.http.routers.uptime-kuma.rule: "Host(`status.${NOVA_DOMAIN}`)"
      traefik.http.routers.uptime-kuma.entrypoints: "websecure"
      traefik.http.routers.uptime-kuma.tls: "true"
      traefik.http.routers.uptime-kuma.middlewares: "authelia@file"
      traefik.http.services.uptime-kuma.loadbalancer.server.port: "3001"
      homepage.group: "Infrastructure"
      homepage.name: "Uptime Kuma"
      homepage.icon: "uptime-kuma.svg"
      homepage.href: "https://status.${NOVA_DOMAIN}"
      homepage.description: "Service Uptime Monitoring"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    ports:
      - "3002:3001/tcp"
    restart: "unless-stopped"
    volumes:
      - "uptime_kuma_data:/app/data"

volumes:

  uptime_kuma_data:
    external: true

  vikunja_db:
    external: true

  vikunja_files:
    external: true

  ntfy_data:
    external: true

  actual_data: {}

  # Local budget copy the MCP downloads and syncs back. Disposable/derived — the
  # authoritative data lives in `actual_data` (backed up via the backup stack).
  # If lost, the MCP re-downloads on next start.
  actual_mcp_data: {}

  stirling_config:
    external: true

  snapotter_data:
    external: true

  snapotter_workspace: {}

# Infrastructure Stack - Traefik, Arcane, WUD, DuckDNS, Homepage, Glances, Volume Sharer, Scrutiny
# Usage: ./nova.sh up infra   (or: docker compose -f infra/compose.yaml up -d)
# Requires .env file for: DUCKDNS_TOKEN, NOVA_DOMAIN

name: infra

networks:
  traefik_default:
    external: true
    name: "traefik_default"
  socket_proxy:
    external: true
    name: "socket_proxy"
  # Isolated bridge for self-hosted GitHub Actions runners. Carries traffic between
  # each runner and runners-socket-proxy (write-allowlist). Runners also reach the
  # public internet via this network's default gateway (it is NOT marked internal).
  runners_net:
    driver: bridge
  # Internal-only bridge between sablier (on-demand container controller) and its
  # scoped socket-proxy. No egress; nothing else joins this network. Sablier reaches
  # Traefik on traefik_default separately.
  sablier_internal:
    driver: bridge
    internal: true

services:

  socket-proxy:
    container_name: "socket-proxy"
    environment:
      # Shared read-only proxy for all stacks. POST and DELETE blocked by default.
      - "CONTAINERS=1"
      - "EVENTS=1"
      - "INFO=1"
      - "LOGS=1"
      - "NETWORKS=1"
      - "VERSION=1"
      - "VOLUMES=1"
    image: "tecnativa/docker-socket-proxy"
    networks:
      - "socket_proxy"
    ports:
      # Expose on loopback only for host-networked services (glances, volume-sharer).
      - "127.0.0.1:2375:2375"
    restart: "unless-stopped"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"


  duckdns:
    container_name: "duckdns"
    environment:
      - "SUBDOMAINS=${DUCKDNS_SUBDOMAIN}"
      - "TOKEN=${DUCKDNS_TOKEN}"
    image: "lscr.io/linuxserver/duckdns:latest"
    labels:
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    restart: "unless-stopped"

  homepage:
    container_name: "homepage"
    depends_on:
      - "socket-proxy"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    image: "ghcr.io/gethomepage/homepage:latest"
    environment:
      - "TZ=${TZ}"
      - "HOMEPAGE_ALLOWED_HOSTS=${NOVA_HOSTNAME}:3000,${NOVA_DOMAIN}:3000,home.${NOVA_DOMAIN}"
    labels:
      traefik.enable: "true"
      traefik.http.routers.homepage.rule: "Host(`home.${NOVA_DOMAIN}`)"
      traefik.http.routers.homepage.entrypoints: "websecure"
      traefik.http.routers.homepage.tls: "true"
      traefik.http.routers.homepage.middlewares: "authelia@file"
      traefik.http.services.homepage.loadbalancer.server.port: "3000"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
      - "socket_proxy"
    ports:
      - "3000:3000/tcp"
    restart: "unless-stopped"
    volumes:
      - "../homepage:/app/config"
      - "/data1:/data1:ro"
      - "/data2:/data2:ro"
      - "/data3:/data3:ro"

  arcane:
    container_name: "arcane"
    image: "ghcr.io/getarcaneapp/arcane:latest"
    cgroup: host
    environment:
      - "ENCRYPTION_KEY=${ARCANE_ENCRYPTION_KEY}"
      - "JWT_SECRET=${ARCANE_JWT_SECRET}"
      - "TZ=${TZ}"
    healthcheck:
      disable: true  # distroless image has no shell, wget, or curl; app health confirmed via logs
    labels:
      traefik.enable: "true"
      traefik.http.routers.arcane.rule: "Host(`arcane.${NOVA_DOMAIN}`)"
      traefik.http.routers.arcane.entrypoints: "websecure"
      traefik.http.routers.arcane.tls: "true"
      traefik.http.routers.arcane.middlewares: "authelia@file"
      traefik.http.services.arcane.loadbalancer.server.port: "3552"
      homepage.group: "Infrastructure"
      homepage.name: "Arcane"
      homepage.icon: "arcane.svg"
      homepage.href: "https://arcane.${NOVA_DOMAIN}"
      homepage.description: "Container Management"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    ports:
      - "3552:3552/tcp"
    restart: "unless-stopped"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "arcane_data:/app/data"

  dockge:
    container_name: "dockge"
    image: "louislam/dockge:1"
    # Identity-mapped stacks dir: the host path is mounted at the SAME path
    # inside the container so that relative bind-mounts in stack compose files
    # (e.g. ../decluttarr/config.yaml) resolve to a path that exists on the
    # host too. Without this, Dockge resolves ../foo against /opt/stacks/,
    # which the host docker daemon then auto-creates as an empty dir, silently
    # breaking any bind-mounted config files.
    environment:
      - "DOCKGE_STACKS_DIR=${NOVA_CONFIG_PATH}"
    healthcheck:
      # Dockge image is Node-based and ships without wget/curl; use node http instead.
      test:
        - "CMD"
        - "node"
        - "-e"
        - "require('http').get('http://localhost:5001/', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      traefik.enable: "true"
      traefik.http.routers.dockge.rule: "Host(`dockge.${NOVA_DOMAIN}`)"
      traefik.http.routers.dockge.entrypoints: "websecure"
      traefik.http.routers.dockge.tls: "true"
      traefik.http.routers.dockge.middlewares: "authelia@file"
      traefik.http.services.dockge.loadbalancer.server.port: "5001"
      homepage.group: "Infrastructure"
      homepage.name: "Dockge"
      homepage.icon: "dockge.png"
      homepage.href: "https://dockge.${NOVA_DOMAIN}"
      homepage.description: "Compose stack manager"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    restart: "unless-stopped"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "dockge_data:/app/data"
      - "${NOVA_CONFIG_PATH}:${NOVA_CONFIG_PATH}"

  traefik:
    command:
      - "--providers.docker=true"
      - "--providers.docker.endpoint=tcp://socket-proxy:2375"
      - "--providers.docker.exposedByDefault=false"
      - "--providers.docker.network=traefik_default"
      - "--providers.file.filename=/etc/traefik/dynamic.yaml"
      - "--providers.file.watch=true"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge.provider=duckdns"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge.disablepropagationcheck=true"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge.delaybeforecheck=60"
      - "--certificatesresolvers.letsencrypt.acme.dnschallenge.resolvers=1.1.1.1:53,1.0.0.1:53"
      - "--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL}"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      - "--api.dashboard=true"
      - "--ping=true"
      - "--ping.entrypoint=ping"
      - "--entrypoints.ping.address=:8082"
      # Sablier plugin: on-demand container spin-up. Version pinned in step with
      # sablierapp/sablier daemon (below). Bumping requires editing BOTH the plugin
      # version here and the daemon image tag on the sablier service.
      - "--experimental.plugins.sablier.moduleName=github.com/sablierapp/sablier-traefik-plugin"
      - "--experimental.plugins.sablier.version=v1.3.0"
    container_name: "traefik"
    depends_on:
      - "socket-proxy"
    environment:
      - "DUCKDNS_TOKEN=${DUCKDNS_TOKEN}"
      - "NOVA_DOMAIN=${NOVA_DOMAIN}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8082/ping >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    image: "traefik:v3.6"
    labels:
      traefik.enable: "true"
      traefik.http.routers.traefik-dashboard.rule: "Host(`traefik.${NOVA_DOMAIN}`)"
      traefik.http.routers.traefik-dashboard.entrypoints: "websecure"
      traefik.http.routers.traefik-dashboard.tls: "true"
      traefik.http.routers.traefik-dashboard.tls.certresolver: "letsencrypt"
      traefik.http.routers.traefik-dashboard.tls.domains[0].main: "${NOVA_DOMAIN}"
      traefik.http.routers.traefik-dashboard.tls.domains[0].sans: "*.${NOVA_DOMAIN}"
      traefik.http.routers.traefik-dashboard.service: "api@internal"
      traefik.http.routers.traefik-dashboard.middlewares: "authelia@file"
      traefik.http.routers.root-redirect.rule: "Host(`${NOVA_DOMAIN}`)"
      traefik.http.routers.root-redirect.entrypoints: "websecure"
      traefik.http.routers.root-redirect.tls: "true"
      traefik.http.routers.root-redirect.middlewares: "root-to-homepage"
      traefik.http.routers.root-redirect.service: "noop@internal"
      traefik.http.middlewares.root-to-homepage.redirectregex.regex: "^https://${NOVA_DOMAIN}/(.*)"
      traefik.http.middlewares.root-to-homepage.redirectregex.replacement: "https://home.${NOVA_DOMAIN}/$${1}"
      traefik.http.middlewares.root-to-homepage.redirectregex.permanent: "true"
      homepage.group: "Infrastructure"
      homepage.name: "Traefik"
      homepage.icon: "traefik.svg"
      homepage.href: "http://traefik.${NOVA_DOMAIN}"
      homepage.description: "Reverse Proxy Dashboard"
      wud.watch: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
      - "socket_proxy"
    ports:
      - "80:80/tcp"
      - "443:443/tcp"
    restart: "unless-stopped"
    volumes:
      - "../traefik/dynamic.yaml:/etc/traefik/dynamic.yaml:ro"
      - "traefik_acme:/letsencrypt"

  glances:
    container_name: "glances"
    depends_on:
      - "socket-proxy"
    environment:
      - "TZ=${TZ}"
      - "GLANCES_OPT=-w"
      - "DOCKER_HOST=tcp://127.0.0.1:2375"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:61208/api/4/uptime >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    image: "nicolargo/glances:latest-full"
    labels:
      homepage.group: "Infrastructure"
      homepage.name: "Glances"
      homepage.icon: "glances.png"
      homepage.href: "https://glances.${NOVA_DOMAIN}"
      homepage.description: "System Monitoring"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    network_mode: "host"
    pid: "host"
    restart: "unless-stopped"
    volumes:
      - "/:/rootfs:ro"

  volume-sharer:
    container_name: "volume-sharer"
    depends_on:
      - "socket-proxy"
    environment:
      # USERID/GROUPID control the Samba user inside the container.
      # Root (0) is required to read all volume contents regardless of ownership.
      # Set to ${PUID}/${PGID} if all your volumes are user-owned and you want
      # to restrict access, accepting that root-owned volumes will be unreadable.
      - "USERID=${SAMBA_UID:-0}"
      - "GROUPID=${SAMBA_GID:-0}"
      - "DOCKER_HOST=tcp://127.0.0.1:2375"
    hostname: "${NOVA_HOSTNAME}"
    image: "gdiepen/volume-sharer"
    labels:
      wud.watch: "false"
    network_mode: "host"
    ports:
      - "139:139/tcp"
      - "445:445/tcp"
    restart: "unless-stopped"
    volumes:
      - "/var/lib/docker/volumes:/docker_volumes"
      - "samba_config:/etc/samba"

  wud:
    container_name: "wud"
    environment:
      - "WUD_LOG_FORMAT=text"
      # Watcher - WUD v8 format: WUD_WATCHER_{NAME}_{OPTION} (no DOCKER_ prefix; type inferred from SOCKET)
      - "WUD_WATCHER_LOCAL_SOCKET=/var/run/docker.sock"
      - "WUD_WATCHER_LOCAL_CRON=0 4 * * *"
      - "WUD_WATCHER_LOCAL_WATCHBYDEFAULT=false"
      # Triggers:
      #   discord.notify — default for third-party images; human-in-the-loop deploys via
      #     Arcane UI or `nova.sh update`. Avoids silent breakage from upstream surprises
      #     (see commit f5f901c for the history that motivated this).
      #   docker.local   — opt-in auto-recreate for images we publish ourselves
      #     (ghcr.io/nova-firefly/*, ghcr.io/kjsb25/*). Safe because we control when
      #     changes ship via CI.
      - "WUD_TRIGGER_DISCORD_NOTIFY_URL=${DISCORD_WEBHOOK_URL}"
      - "WUD_TRIGGER_DISCORD_NOTIFY_ONCE=true"
      - "WUD_TRIGGER_DOCKER_LOCAL_PRUNE=true"
      # Registries - lscr.io (LinuxServer) images not auto-detected without this
      - "WUD_REGISTRY_LSCR_PUBLIC_URL=https://lscr.io"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    image: "getwud/wud"
    labels:
      traefik.enable: "true"
      traefik.http.routers.wud.rule: "Host(`wud.${NOVA_DOMAIN}`)"
      traefik.http.routers.wud.entrypoints: "websecure"
      traefik.http.routers.wud.tls: "true"
      traefik.http.routers.wud.middlewares: "authelia@file"
      traefik.http.services.wud.loadbalancer.server.port: "3000"
      homepage.group: "Infrastructure"
      homepage.name: "WUD"
      homepage.icon: "whats-up-docker.png"
      homepage.href: "http://wud.${NOVA_DOMAIN}"
      homepage.description: "Container Update Notifications"
      wud.watch: "false"
    networks:
      - "traefik_default"
    ports:
      - "3003:3000/tcp"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"

  scrutiny:
    container_name: "scrutiny"
    cap_add:
      - "SYS_RAWIO"       # Required to issue ATA/SCSI passthrough commands for SMART
      - "SYS_ADMIN"       # Required for NVMe SMART access on some kernels
    devices:
      - "/dev/sda:/dev/sda"
      - "/dev/sdb:/dev/sdb"
      - "/dev/sdc:/dev/sdc"
      - "/dev/sdd:/dev/sdd"
    environment:
      - "TZ=${TZ}"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/api/health >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    image: "ghcr.io/analogj/scrutiny:master-omnibus"
    labels:
      traefik.enable: "true"
      traefik.http.routers.scrutiny.rule: "Host(`scrutiny.${NOVA_DOMAIN}`)"
      traefik.http.routers.scrutiny.entrypoints: "websecure"
      traefik.http.routers.scrutiny.tls: "true"
      traefik.http.routers.scrutiny.middlewares: "authelia@file"
      traefik.http.services.scrutiny.loadbalancer.server.port: "8080"
      homepage.group: "Infrastructure"
      homepage.name: "Scrutiny"
      homepage.icon: "scrutiny.png"
      homepage.href: "https://scrutiny.${NOVA_DOMAIN}"
      homepage.description: "Hard Drive Health (S.M.A.R.T.)"
      wud.watch: "true"
      wud.watch.digest: "true"
      wud.trigger.include: "discord.notify"
    networks:
      - "traefik_default"
    ports:
      - "8082:8080/tcp"
    restart: "unless-stopped"
    volumes:
      - "../scrutiny/scrutiny.yaml:/opt/scrutiny/config/scrutiny.yaml:ro"
      - "scrutiny_data:/opt/scrutiny/data"
      - "/run/udev:/run/udev:ro"

  # --- On-demand container spin-up (Sablier + scoped socket proxy) ---
  # Sablier stops idle containers and restarts them on the next HTTP request via
  # Traefik. Target services opt in with `sablier.enable=true` + `sablier.group=<name>`
  # labels plus a Sablier middleware in their Traefik chain (see traefik/dynamic.yaml
  # for the `sablier-tools@file` middleware and context/patterns.md for the pattern).
  #
  # Version pinning: the plugin version (registered on the traefik service above) and
  # the daemon image tag here must stay compatible. Sablier follows semver — pin both,
  # bump together, verify the wake flow before merging.

  socket-proxy-sablier:
    container_name: "socket-proxy-sablier"
    # Write-allowlist socket proxy scoped to just what Sablier needs: list containers
    # by label, inspect them, and start/stop them. No IMAGES / NETWORKS / VOLUMES /
    # EXEC / BUILD / SYSTEM / SECRETS — a compromised Sablier can only cycle the
    # containers it already manages, nothing else.
    environment:
      - "CONTAINERS=1"
      - "INFO=1"
      - "VERSION=1"
      - "EVENTS=1"
      - "POST=1"
    image: "tecnativa/docker-socket-proxy"
    cap_drop:
      - "ALL"
    security_opt:
      - "no-new-privileges:true"
    labels:
      wud.watch: "false"
    networks:
      - "sablier_internal"
    restart: "unless-stopped"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"

  sablier:
    container_name: "sablier"
    image: "sablierapp/sablier:1.15.0"
    command:
      - "start"
      - "--provider.name=docker"
    environment:
      - "DOCKER_HOST=tcp://socket-proxy-sablier:2375"
    healthcheck:
      # Sablier ships FROM scratch — no shell, no curl. Its binary is at /bin/sablier
      # and has a `health` subcommand that hits http://localhost:10000/health by default.
      test: ["CMD", "/bin/sablier", "health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      wud.watch: "true"
      wud.trigger.include: "discord.notify"
    networks:
      # traefik_default so Traefik can reach http://sablier:10000 for the plugin API.
      # sablier_internal so sablier can reach the scoped socket proxy.
      - "traefik_default"
      - "sablier_internal"
    restart: "unless-stopped"

  # --- Self-hosted GitHub Actions runners + supporting sidecars ---
  # See context/runners.md for architecture, add-a-runner steps, and PAT rotation.
  #
  # Image digests intentionally use a placeholder (sha256:000…). Replace before first
  # `nova.sh up infra` with the current upstream digest — instructions in runners.md.
  # Docker will refuse to pull until a real digest is supplied, which is the point:
  # digest pinning is a hard precondition for these write-capable containers.

  runners-socket-proxy:
    container_name: "runners-socket-proxy"
    # Write-capable socket proxy used by runner containers to launch job containers
    # via Docker. POST=1 enables creates/starts; EXEC, BUILD, SECRETS, SYSTEM remain
    # off so a compromised runner cannot exec into other containers, build arbitrary
    # images, read swarm secrets, or shut down the daemon.
    environment:
      - "CONTAINERS=1"
      - "IMAGES=1"
      - "NETWORKS=1"
      - "VOLUMES=1"
      - "POST=1"
      - "INFO=1"
      - "VERSION=1"
    image: "tecnativa/docker-socket-proxy"
    cap_drop:
      - "ALL"
    security_opt:
      - "no-new-privileges:true"
    labels:
      wud.watch: "false"
    networks:
      - "runners_net"
    restart: "unless-stopped"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"

  nova-config-sync:
    container_name: "nova-config-sync"
    # Sole writer to /srv/nova-config — replaces the old nova-config/.github/workflows/sync.yml
    # GitHub Actions round-trip with an in-host 10-minute reconciliation loop.
    # The runner containers bind-mount /srv/nova-config read-only; this sidecar is
    # the only process permitted to mutate that path.
    #
    # alpine/git has ENTRYPOINT ["git"] baked in — we override it so the loop
    # script runs under /bin/sh instead of being passed as a git subcommand.
    image: "alpine/git@sha256:fac7bc2c12aa52bff1c78a26359990e149954a60cdd5d6e20e7e009666c73e0a"
    init: true
    cap_drop:
      - "ALL"
    security_opt:
      - "no-new-privileges:true"
    entrypoint:
      - "/bin/sh"
      - "-c"
    command:
      - |
        set -eu
        while :; do
          if [ ! -d /repo/.git ]; then
            # Bootstrap: /repo is empty (or missing .git). Clone into it so the
            # runners' read-only mount has something to serve. Uses --depth 1
            # since we never need history on the host — the sidecar only tracks
            # origin/main. If nova-config is ever flipped private, this clone
            # will need a token (future problem).
            git clone --depth 1 https://github.com/nova-firefly/nova-config /repo \
              || echo "nova-config-sync: initial clone failed, will retry in 600s"
          else
            cd /repo \
              && git fetch --quiet origin main \
              && git reset --hard origin/main \
              || echo "nova-config-sync: reconcile failed, will retry in 600s"
          fi
          sleep 600
        done
    labels:
      wud.watch: "false"
    restart: "unless-stopped"
    volumes:
      - "/srv/nova-config:/repo"

  runner-nova-config:
    container_name: "runner-nova-config"
    image: "myoung34/github-runner@sha256:67c2447f2717227277560b76ba7e281e9f981d13508fe934b9f9f3cfd68e2d52"
    init: true
    cap_drop:
      - "ALL"
    # The image starts as root, `chown -R runner:runner /actions-runner`, then
    # su-exec's down to uid 1001. That init sequence needs CHOWN + FOWNER (fix
    # ownership), DAC_OVERRIDE (root writing into the freshly-chown'd dir), and
    # SETUID + SETGID (privilege drop). Without them the runner user cannot
    # write .env / .path / _diag, and Runner.Listener aborts with exit 134.
    # These caps are only exercised during ~1s of container startup — the runner
    # process itself does not need them and workflow jobs run under su-exec.
    cap_add:
      - "CHOWN"
      - "DAC_OVERRIDE"
      - "FOWNER"
      - "SETUID"
      - "SETGID"
    security_opt:
      - "no-new-privileges:true"
    tmpfs:
      - "/tmp"
      - "/_work"
    environment:
      - "ACCESS_TOKEN=${GH_PAT}"
      - "REPO_URL=https://github.com/nova-firefly/nova-config"
      - "RUNNER_NAME=runner-nova-config"
      - "RUNNER_SCOPE=repo"
      - "LABELS=nova,nova-config"
      - "EPHEMERAL=true"
      - "DISABLE_AUTO_UPDATE=true"
      - "RUNNER_WORKDIR=/_work"
      - "DOCKER_HOST=tcp://runners-socket-proxy:2375"
    labels:
      wud.watch: "false"
    networks:
      - "runners_net"
    restart: "unless-stopped"
    volumes:
      - "/srv/nova-config:/nova-config:ro"
      - "runner_nova_config_state:/runner"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "512M"

  runner-vibe-kanban-tools:
    container_name: "runner-vibe-kanban-tools"
    image: "myoung34/github-runner@sha256:67c2447f2717227277560b76ba7e281e9f981d13508fe934b9f9f3cfd68e2d52"
    init: true
    cap_drop:
      - "ALL"
    # The image starts as root, `chown -R runner:runner /actions-runner`, then
    # su-exec's down to uid 1001. That init sequence needs CHOWN + FOWNER (fix
    # ownership), DAC_OVERRIDE (root writing into the freshly-chown'd dir), and
    # SETUID + SETGID (privilege drop). Without them the runner user cannot
    # write .env / .path / _diag, and Runner.Listener aborts with exit 134.
    # These caps are only exercised during ~1s of container startup — the runner
    # process itself does not need them and workflow jobs run under su-exec.
    cap_add:
      - "CHOWN"
      - "DAC_OVERRIDE"
      - "FOWNER"
      - "SETUID"
      - "SETGID"
    security_opt:
      - "no-new-privileges:true"
    tmpfs:
      - "/tmp"
      - "/_work"
    environment:
      - "ACCESS_TOKEN=${GH_PAT}"
      - "REPO_URL=https://github.com/nova-firefly/vibe-kanban-tools"
      - "RUNNER_NAME=runner-vibe-kanban-tools"
      - "RUNNER_SCOPE=repo"
      - "LABELS=nova,vibe-kanban-tools"
      - "EPHEMERAL=true"
      - "DISABLE_AUTO_UPDATE=true"
      - "RUNNER_WORKDIR=/_work"
      - "DOCKER_HOST=tcp://runners-socket-proxy:2375"
    labels:
      wud.watch: "false"
    networks:
      - "runners_net"
    restart: "unless-stopped"
    volumes:
      - "/srv/nova-config:/nova-config:ro"
      - "runner_vibe_kanban_tools_state:/runner"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "512M"

  runner-movienight:
    container_name: "runner-movienight"
    # Single runner serves both movienight and movienight-test workflows via the
    # shared `movienight,movienight-test` labels — see decision #6 in the Phase 1
    # spec (one runner per repo, not per environment).
    image: "myoung34/github-runner@sha256:67c2447f2717227277560b76ba7e281e9f981d13508fe934b9f9f3cfd68e2d52"
    init: true
    cap_drop:
      - "ALL"
    # The image starts as root, `chown -R runner:runner /actions-runner`, then
    # su-exec's down to uid 1001. That init sequence needs CHOWN + FOWNER (fix
    # ownership), DAC_OVERRIDE (root writing into the freshly-chown'd dir), and
    # SETUID + SETGID (privilege drop). Without them the runner user cannot
    # write .env / .path / _diag, and Runner.Listener aborts with exit 134.
    # These caps are only exercised during ~1s of container startup — the runner
    # process itself does not need them and workflow jobs run under su-exec.
    cap_add:
      - "CHOWN"
      - "DAC_OVERRIDE"
      - "FOWNER"
      - "SETUID"
      - "SETGID"
    security_opt:
      - "no-new-privileges:true"
    tmpfs:
      - "/tmp"
      - "/_work"
    environment:
      - "ACCESS_TOKEN=${GH_PAT}"
      - "REPO_URL=https://github.com/nova-firefly/movienight"
      - "RUNNER_NAME=runner-movienight"
      - "RUNNER_SCOPE=repo"
      - "LABELS=nova,movienight,movienight-test"
      - "EPHEMERAL=true"
      - "DISABLE_AUTO_UPDATE=true"
      - "RUNNER_WORKDIR=/_work"
      - "DOCKER_HOST=tcp://runners-socket-proxy:2375"
    labels:
      wud.watch: "false"
    networks:
      - "runners_net"
    restart: "unless-stopped"
    volumes:
      - "/srv/nova-config:/nova-config:ro"
      - "runner_movienight_state:/runner"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "512M"

  runner-todoassist:
    container_name: "runner-todoassist"
    image: "myoung34/github-runner@sha256:67c2447f2717227277560b76ba7e281e9f981d13508fe934b9f9f3cfd68e2d52"
    init: true
    cap_drop:
      - "ALL"
    # The image starts as root, `chown -R runner:runner /actions-runner`, then
    # su-exec's down to uid 1001. That init sequence needs CHOWN + FOWNER (fix
    # ownership), DAC_OVERRIDE (root writing into the freshly-chown'd dir), and
    # SETUID + SETGID (privilege drop). Without them the runner user cannot
    # write .env / .path / _diag, and Runner.Listener aborts with exit 134.
    # These caps are only exercised during ~1s of container startup — the runner
    # process itself does not need them and workflow jobs run under su-exec.
    cap_add:
      - "CHOWN"
      - "DAC_OVERRIDE"
      - "FOWNER"
      - "SETUID"
      - "SETGID"
    security_opt:
      - "no-new-privileges:true"
    tmpfs:
      - "/tmp"
      - "/_work"
    environment:
      - "ACCESS_TOKEN=${GH_PAT}"
      - "REPO_URL=https://github.com/nova-firefly/todoassist"
      - "RUNNER_NAME=runner-todoassist"
      - "RUNNER_SCOPE=repo"
      - "LABELS=nova,todoassist"
      - "EPHEMERAL=true"
      - "DISABLE_AUTO_UPDATE=true"
      - "RUNNER_WORKDIR=/_work"
      - "DOCKER_HOST=tcp://runners-socket-proxy:2375"
    labels:
      wud.watch: "false"
    networks:
      - "runners_net"
    restart: "unless-stopped"
    volumes:
      - "/srv/nova-config:/nova-config:ro"
      - "runner_todoassist_state:/runner"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: "512M"

volumes:

  traefik_acme:
    external: true

  samba_config:
    external: true

  scrutiny_data:
    external: true

  arcane_data:

  dockge_data:

  # Per-runner registration / settings state — survives container recreate so the
  # runner does not need to re-register on every restart.
  runner_nova_config_state:

  runner_vibe_kanban_tools_state:

  runner_movienight_state:

  runner_todoassist_state:
